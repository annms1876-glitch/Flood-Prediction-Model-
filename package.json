{
  "name": "flood-prediction-model",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "floodlens-ui"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=floodlens-ui",
    "build": "npm run build --workspace=floodlens-ui",
    "start": "npm run start --workspace=floodlens-ui",
    "lint": "npm run lint --workspace=floodlens-ui"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "eslint-config-next": "^16.3.4"
  },
  "dependencies": {
    "@types/google.maps": "^3.66.2",
    "@vis.gl/react-google-maps": "^1.10.0",
    "firebase": "^12.18.0",
    "framer-motion": "^13.2.0"
  }
}
